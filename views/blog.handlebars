<!-- Edit existing post created by logged user -->
<div class="text-left">
  <h2>Edit Post</h2>
  <h3>{{title}}</h3>
  <h3>{{content}}</h3>
  <p>
    <button class="btn btn-primary">Update Post</button>
  </p>
    <p>
    <button class="btn btn-primary">Delete</button>
  </p>
</div>


<!-- Add comments to existing blogs -->
<div class="text-left">
  <h2>{{title}}</h2>
  <h2>{{content}}</h2>
</div>
<div class="row mt-4">
  <div class="col-md-8">
    <form class="form existing-blog-form">
      <div class="form-group">
        <label for="blog-comment">Comment</label>
        <textarea class="form-input" id="blog-comment" name="blog-comment"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Submit</button>
      </div>
    </form>
  </div>
